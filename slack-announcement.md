*devorch update: Cleaner git history*

Updated the installer to automatically gitignore `verification/` and `implementation/` folders in the devorch directory. This prevents all those task verification reports and implementation logs from cluttering your commits.

Run `devorch update && devorch install` to get the update.
