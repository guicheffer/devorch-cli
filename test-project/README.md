# Test Project

This is a test project used for verifying devorch installation and functionality during development.

## Purpose

- Test devorch CLI commands
- Verify template compilation and installation
- Validate dependency resolution
- Test configuration management

## Configuration

See `devorch.config.yml` for the current setup.

## Bootstrap

Run `bun ../src/cli/index.ts --local=../`
