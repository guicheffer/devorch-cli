User needs to have the Figma dev mode MCP installed.

**STOP** if Figma MCP is not installed. Tell the user:

"Install the Figma MCP first: `claude mcp add --transport http figma https://mcp.figma.com/mcp`"
